/* eslint-disable no-mixed-spaces-and-tabs */

import ProductsList from '../../components/ProductsList';

const TopViews = () => {
	return <ProductsList sortBasedOn={'views'} />;
};

export default TopViews;
